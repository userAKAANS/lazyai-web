export default function Message({ role, content }: any) {
  return (
    <div className={`py-2 px-4 my-2 rounded-lg max-w-2xl ${
      role === "user" ? "bg-blue-600/20 self-end" : "bg-neutral-900"
    }`}>
      <strong className="block text-xs text-neutral-400 mb-1">
        {role === "user" ? "You" : "LazyAI"}
      </strong>
      <p className="text-neutral-100 whitespace-pre-wrap">
        {content}
      </p>
    </div>
  );
}
